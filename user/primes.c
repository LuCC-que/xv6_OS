#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"
#define MAX 35;
#define CUR 2;
int main(void)
{

  printf("primes lab");

  for (int i = 2; i <= MAX; ++i)
  {
  }

  exit(0);
}